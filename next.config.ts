import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // /* config options here */
  // reactStrictMode: true, // Enable React Strict Mode
  // pageExtensions: ['js', 'jsx', 'ts', 'tsx'], // Include .ts and .tsx extensions for pages
};

export default nextConfig;
